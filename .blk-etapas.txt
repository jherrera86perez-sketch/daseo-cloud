        {isAdmin && (
          <Card>
            <CardHeader>
              <CardTitle>{ts("title")}</CardTitle>
            </CardHeader>
            <CardContent>
              <StagesEditor
                stages={board.map((b) => ({
                  id: b.stage.id,
                  name: b.stage.name,
                  isWon: b.stage.isWon,
                  isLost: b.stage.isLost,
                  dealCount: b.deals.length,
                }))}
              />
            </CardContent>
          </Card>
        )}